import {
	version
} from '../package.json'

const setting = {
	// 侧边菜单默认折叠状态
	menu: {
		menuCollapse: false
	},
	// 主题默认值
	theme: {
		name: 'theme-dark3'
	},

	// 版本
	releases: {
		version: version

	},
	page: {
		opened : []
	} 

}

export default setting
